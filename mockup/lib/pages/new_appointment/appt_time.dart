import 'package:flutter/material.dart';

class AppontmentTime extends StatelessWidget {
  const AppontmentTime({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Choose your Time:');
  }
}
