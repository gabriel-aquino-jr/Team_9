import 'package:flutter/material.dart';

class AppontmentCity extends StatelessWidget {
  const AppontmentCity({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Choose the City:');
  }
}
